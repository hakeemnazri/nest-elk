input {
  tcp {
    port => 5001
  }
  beats {
    port => 5002
  }
}


filter {
  json {
    source => "message"
  }
}
output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "app-%{+YYYY.MM.dd}"
  }
  stdout { }
}